services:
  mnemory:
    build: .
    ports:
      - "8050:8050"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - mnemory-data:/data
    restart: unless-stopped

volumes:
  mnemory-data:
